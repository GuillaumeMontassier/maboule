{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace=server"
  },
  "deploy": {
    "startCommand": "npm run migrate:prod --workspace=server && npm run start --workspace=server"
  }
}
